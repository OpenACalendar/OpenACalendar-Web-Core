{#
 # @package Core
 # @link http://ican.openacalendar.org/ OpenACalendar Open Source Software - Website
 # @license http://ican.openacalendar.org/license.html 3-clause BSD
 # @copyright (c) JMB Technology Limited, http://jmbtechnology.co.uk/
 # @author James Baster <james@jarofgreen.co.uk>
 #}
<div style="padding: 5px; margin: 5px 0 5px 0; background:  {{ themeVariables.colourHistoryGroupBackground  }}; border-radius: 5px;">
	<div style="font-weight: bold;">{%  if historyItem.isNew %}New {%  endif %}Group: <a href="{{ config.getWebSiteDomainSecure(site.slug) }}/group/{{ historyItem.groupSlug }}">{{ historyItem.title }}</a></div>
	{% if historyItem.isAnyChangeFlagsUnknown %}
		<div>{{ historyItem.description | nl2br  }}</div>
	{% else %}
		{% if historyItem.getTitleChanged %}
			<div>
				<div>Title {%  if not historyItem.isNew %} changed{%  endif %}:</div>
				<div style="font-size: 10pt;">{{ historyItem.title }}</div>
			</div>
		{%endif %}
		{% if historyItem.getDescriptionChanged %}
			<div>
				<div>Description {%  if not historyItem.isNew %} changed{%  endif %}:</div>
				<div style="font-size: 10pt;">{{ historyItem.description }}</div>
			</div>
		{%endif %}
		{% if historyItem.getUrlChanged %}
			<div>
				<div>URL {%  if not historyItem.isNew %} changed{%  endif %}:</div>
				<div style="font-size: 10pt;">{{ historyItem.url }}</div>
			</div>
		{%endif %}
		{% if historyItem.getTwitterUsernameChanged %}
			<div>
				<div>Twitter {%  if not historyItem.isNew %} changed{%  endif %}:</div>
				<div style="font-size: 10pt;">{{ historyItem.twitterUsername }}</div>
			</div>
		{%endif %}
		{% if historyItem.getIsDeletedChanged %}
			<div>
				<div>Status {%  if not historyItem.isNew %} changed{%  endif %}:</div>
				<div style="font-size: 10pt;">{% if historyItem.getIsDeleted %}Deleted!{% else %}Restored!{% endif %}</div>
			</div>
		{%endif %}
	{% endif %}
	<div style="font-style: italic; font-size: 0.8em;">
		Edited at 
		<time datetime="{{ historyItem.getCreatedAt|tolocaltimeformatted('c', currentTimeZone) }}">
			{{ historyItem.getCreatedAt|tolocaltimeformatted12or24hourclock('D jS M Y g:ia', 'D jS M Y H:i', currentUserClock12Hour, currentTimeZone) }}
		</time>	
		{% if historyItem.userAccountID %}by {{  historyItem.userAccountUserName }}{% endif %}
	</div>
</div>
